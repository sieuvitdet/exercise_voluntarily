import 'dart:ui';

class AppColors {
  AppColors._();

  // Primary colors
  static const Color primary = Color(0xFF4A90E2);
  static const Color secondary = Color(0xFF50C878);
  static const Color accent = Color(0xFFFFD93D);

  // Background gradient colors
  static const Color gradientStart = Color(0xFF1E3C72);
  static const Color gradientEnd = Color(0xFF2A5298);

  // Quality tag colors
  static const Color qualityCasual = Color(0xFFFF6B6B);
  static const Color qualityGood = Color(0xFFFFD93D);
  static const Color qualityExcellent = Color(0xFF6BCF7F);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textDark = Color(0xFF1A1A1A);

  // Surface colors
  static const Color surface = Color(0xFF2D2D2D);
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFF1E3C72);

  // Button colors
  static const Color buttonDisabled = Color(0xFF666666);
}
